# =============================================================================
# Angela AI - Root requirements (thin delegate)
# =============================================================================
# The single source of truth for dependencies is apps/backend/pyproject.toml,
# which defines granular feature groups (ml/vector/data/media/audio/gpu/cache/
# google/docs/...) and user tiers (standard/dev/full).
#
# This file exists for tools that hardcode `pip install -r requirements.txt`
# (setup.py, scripts/run_angela.py, INSTALLATION_GUIDE.md). It installs the
# full-feature runtime. For a lighter base-only install use:
#   pip install -e ./apps/backend
# For contributor tooling (pytest/lint/typecheck) use:
#   pip install -e "./apps/backend[dev]"
# =============================================================================

-e ./apps/backend[standard]
