name = agent_poller
description = Angela AI Polling Bridge (server-side)
version = 1.0
# Per-world settings (set in minetest.conf or world.mt):
#   agent_poller_bridge_url = http://localhost:30003  # Python bridge URL
#   agent_poller_allow_give = false                   # test-only /give backdoor
