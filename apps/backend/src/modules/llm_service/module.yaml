name: llm_service
version: 1.0.0
kind: service
description: LLM backends management — OpenAI, Anthropic, Ollama, Google, LlamaCpp
depends_on:
  required: []
  optional: []
provides:
  services:
    - name: llm_service
      type: singleton
lifecycle:
  init: modules.llm_service.init
  start: modules.llm_service.start
  stop: modules.llm_service.stop
  health:
    endpoint: /health/llm
