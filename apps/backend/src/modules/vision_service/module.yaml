name: vision_service
version: 1.0.0
kind: service
description: Image understanding, object detection, OCR, multimodal perception
depends_on:
  required: []
  optional: []
provides:
  services:
    - name: vision_service
      type: singleton
lifecycle:
  init: modules.vision_service.init
  start: modules.vision_service.start
  stop: modules.vision_service.stop
  health:
    endpoint: /health/vision
