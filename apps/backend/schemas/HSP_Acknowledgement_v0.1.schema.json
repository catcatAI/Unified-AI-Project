{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://unified-ai-project.com/schemas/HSP_Acknowledgement_v0.1.schema.json",
  "title": "HSP Acknowledgement Payload",
  "description": "Schema for the payload of an HSP Acknowledgement message.",
  "type": "object",
  "properties": {
    "status": {
      "description": "The status of the acknowledgement.",
      "type": "string",
      "enum": ["received", "processed"]
    },
    "ack_timestamp": {
      "description": "The ISO 8601 UTC timestamp of when the acknowledgement was generated.",
      "type": "string",
      "format": "date-time"
    },
    "target_message_id": {
      "description": "The ID of the message being acknowledged.",
      "type": "string",
      "format": "uuid"
    }
  },
  "required": ["status", "ack_timestamp", "target_message_id"]
}
