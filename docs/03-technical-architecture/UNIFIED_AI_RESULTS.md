# 統一引擎全面評估結果（UNIFIED_AI_RESULTS）

> 日期：2026-08-22（**污染修正後的乾淨基準**，誠實實測）
> 原則：只看一個數據會失準——**全面列出所有指標**，逐一與其他 AI 比對，
> 判斷是否在正確路線、優缺點、可改善點。
> 所有數字皆為本機實測（除註明「文獻值」者）。
>
> ## ⚠️ 重大更正（2026-08-22 留出集污染）
> **先前所有 bpc 數字（2.331/2.338/2.429 等）是污染值**：訓練循環
> `while read < target` 用 4MB chunk 讀到 96.4MB（溢位 6.4MB），之後在
> 90MB 處取「留出集」實際落在已訓練區內。乾淨測量（`trainer.train_exact_bytes`
> 精確邊界 + holdout 從邊界起）：
>
> | 語料 | 污染值（作廢） | **乾淨值** |
> |---|---|---|
> | enwik9 10MB | ~~2.429~~ | **3.741** |
> | enwik9 90MB | ~~2.331~~ | **2.403** |
> | full_train (90M+alpaca) | ~~2.338~~ | **2.711**（96.5M 處） |
>
> **誠實結論**：仍超過 gzip（2.951），但**尚未達 bz2（2.333）**——差距 0.07。
> 「超過 gzip」的核心宣稱不變，「超過 bz2」撤回。

---

## 1. 統一引擎全面指標（實測）

| # | 指標 | 實測值 | 量測方式 |
|---|---|---|---|
| 1 | enwik9 bpc（訓 10MB 精確邊界） | **3.741** | 乾淨留出集 20KB |
| 2 | enwik9 bpc（訓 90MB 精確邊界） | **2.403** | 乾淨留出集 20KB |
| 3 | full_train bpc（90M+alpaca） | **2.711** | 真留出集（96.5M 處，未訓練區） |
| 4 | 對照：gzip / bz2 / lzma | 2.951 / 2.333 / 2.178 | 同段語料 |
| 5 | alpaca ppl（訓 10k, 測 1k） | **5.5** | 隨機 256, 46x |
| 6 | **壓縮比（全量 91M / 259M）** | **0.35x** | 語料<模型；enwik9 954M 時 3.68x |
| 7 | model_bytes（真實記憶體） | **258.75 MiB** | Cq1024 後，tracemalloc 1.00x |
| 8 | 訓練速度 | **3.3 MiB/s** | 90M/28s, 4MB chunk |
| 9 | 對話算術/邏輯 | **1.0 / 1.0** | 確定性路由，邏輯統計 0.626 |
| 10 | 推理速度 | **4,400 bytes/s** | next_byte_probs 逐 byte |
| 11 | 數學留出集正確率 | **1.000** | 500 未見四則運算（確定性層） |
| 12 | 邏輯留出集正確率 | **0.626** | 500 未見布林命題（統計層，隨機=0.5） |
| 13 | 影像泛化 | **23.9x** | 未見 ramp 型困惑度/已學型 |
| 14 | 音訊泛化 | **83.8x** | 未見 220Hz 困惑度/已學頻率 |

## ✅ 實際輸出能力（2026-08-23 實測，路由分層）

| 能力 | 實測 | 路由 | 狀態 |
|---|---|---|---|
| 精確算術（任意位數） | `752*851=639952`、`(2+3)*4=20` | deterministic-math | ✅ 完美 |
| 布林邏輯（含中綴 nand/nor/xor/xnor） | `false nand true=true`、`true xnor true=true` | deterministic-logic | ✅ 8/8 |
| 問候反射 | hello/你好/good morning → 自然回覆 | reflex | ✅ |
| **事實問答（新）** | `capital of France?`→Paris、`light speed?`→299,792 km/s、未知問題誠實答「不知道」 | **semantic-qa**（SLS 真梯度層） | ✅ 新增 |
| 位元組結構生成 | 學 PNG 後 magic+IHDR 保留；三語生成 10/10 合法 UTF-8 | statistical-core | ✅ |
| 格式補全 | 學加法表後 `12+`→`12+19=`（格式對、值不保證） | statistical-core | ⚠️ 只形不值 |
| 文本風格續寫 | 局部詞組連貫，10+ bytes 後發散 | statistical-core | ⚠️ 架構限制 |

> 路由優先級：reflex > math/logic > **semantic-qa** > statistical core。
> 語義 QA 層為真學習組件（SharedLatentSpace 對比訓練，loss ~0.001，
> cosine 閾值 0.75 低於即誠實承認不知道），補上了統計核架構性無法
> 表示的「長程語義」缺口。

---

## 2. 與其他 AI 的全面對比

### 2.1 壓縮能力（enwik8 bpc，越低越好）——本機實測

| 模型 | bpc | 相對我們 | 模型大小 | 語料 |
|---|---|---|---|---|
| **統一引擎（訓 90MB 精確邊界）** | **2.403** | 1.00x | 259 MiB 固定 | 90 MB |
| **統一引擎（訓 10MB 精確邊界）** | **3.741** | 1.56x | 259 MiB 固定 | 10 MB |
| gzip（本機實測） | 2.951 | 0.81x（我們贏 18%） | 無固定模型 | 壓縮即時 |
| bz2（本機實測） | 2.333 | 0.97x | 無固定模型 | 壓縮即時 |
| lzma（本機實測） | 2.178 | 0.91x | 無固定模型 | 壓縮即時 |
| PPMd（文獻值） | ~1.48 | 0.62x | ~100 MB | 100 MB |
| LSTM（文獻值） | ~1.30 | 0.54x | ~100 MB | 100 MB |
| Transformer-XL（文獻值） | 0.99 | 0.41x | ~250 MB | 100 MB |
| GPT-3（文獻值） | 0.99 | 0.41x | 175B | 570 GB |
| CTX-LLM 2026 SOTA（文獻值） | 0.53 | 0.22x | ? | ? |

**判讀**：**乾淨 bpc 2.403 超過 gzip(2.951)**，輸 bz2(2.333) 差距 3%、
lzma 差距 9%。「超過 bz2」的舊宣稱撤回（污染數字）。

### 2.1.1 多語言 wiki（2026-08-23，500MB/語，`train_exact` 精確邊界 + `<text>` 乾淨抽取）

| 語種 | 我們 bpc | gzip-ref（同分割同語料） | 判定 |
|---|---|---|---|
| **en (wiki_en)** | **2.505** | 4.152 | ✅ 贏 40% |
| **ja (wiki_ja)** | **3.416** | 4.004 | ✅ 贏 15% |
| **zh (wiki_zh)** | **4.192** | 5.229 | ✅ 贏 20% |

> 三語全勝 gzip。checkpoint：`ZX/checkpoints/unified/wiki_{en,zh,ja}_500mb.npz`。
> zh 的 4.192 高於 en 是 UTF-8 位元組熵本質（3 bytes/char），與同語料
> gzip 對照比較才公平——三語皆贏。

### 2.1.2 槽位升級 128k（2026-08-23，修復碰撞飽和後重訓）

65k 槽在 500MB 下 100% 飽和（每槽均 7999 計數=碰撞湯）。升級
`FixedSizeCore(slots=128k, use_feat=False)`（386MiB）重訓：

| 語種 | 65k bpc | **128k bpc** | 改善 | gzip-ref |
|---|---|---|---|---|
| en | 2.505 | **2.365** | -5.6% | 4.152（贏 43%） |
| ja | 3.416 | **3.223** | -5.6% | 4.004（贏 19%） |
| zh | 4.192 | **3.944** | -5.9% | 5.229（贏 25%） |

> checkpoint：`ZX/checkpoints/unified/wiki_{lang}_500mb_128k.npz`。
> 利用率審計同時證實：65k 槽下 g5 碰撞反噬（g4 單用反而更好），
> 128k 後高階恢復主導——這是「6-gram 變差」舊結論的根因。

### 2.2 壓縮比（corpus/model_bytes，越高越好）——本機實測

| 語料 | corpus | model_bytes（真實） | 壓縮比 |
|---|---|---|---|
| 反覆短句 | 0.5 KB | 259 MiB | < 0.001x |
| alpaca 全量 | 16.3 MiB | 259 MiB | 0.06x |
| enwik8 全量 | 86 MiB | 259 MiB | 0.33x |
| **全量 91M（enwik9 90M+alpaca 10k）** | **91 MiB** | **259 MiB** | **0.35x** |
| **enwik9 全量 954M** | **954 MiB** | **259 MiB** | **3.68x** |
| gzip 對 954 MiB | 壓縮輸出 ~269 MiB | 無固定模型 | 3.55x |
| lzma 對 954 MiB | 壓縮輸出 ~209 MiB | 無固定模型 | 4.56x |

**判讀**：全量 0.35x（語料<模型時<1 正常）；enwik9 954M 時 3.68x **超過 gzip**，輸 lzma。模型固定→壓縮比線性成長成立。

### 2.3 泛化/正確率

| 能力 | 統一引擎 | LLM（如 GPT-3.5/LLaMA） | 判讀 |
|---|---|---|---|
| 四則運算 | **1.000**（確定性層） | 0.90–0.99（可能算錯） | **我們贏**（但那是「非 AI」的 ast 計算） |
| 布林邏輯 | 0.626（統計層） | ~0.95+（推理） | 我們輸 |
| 開放問答 | 亂碼/無意義 | 流利 | 我們大幅輸 |
| 影像/音訊泛化 | 23.9x / 83.8x | 語義理解 | 我們能區分結構，但非理解 |

### 2.4 資源效率

| 指標 | 統一引擎 | LLM | 判讀 |
|---|---|---|---|
| model_bytes 固定 | ✅ 是（259 MiB 不隨語料增長） | ❌ 否 | **我們贏** |
| 訓練成本 | 3.3 MiB/s CPU | 需 GPU 集群 | **我們贏** |
| 推理成本 | 4,400 bytes/s CPU | 需 GPU | 我們贏（但輸在品質） |
| 記憶體 | 259 MiB 固定 | 2 GB–350 GB | **我們贏** |

---

## 3. 模型大小縮放（2GB 目標）——決定性實驗

用戶要求「模型迭代至 2GB 大小」。此實驗在 **256MiB 固定語料**下放大
模型，直接檢驗「放大是否有效」：

| 模型大小 | 槽位 | gram 階 | bpc（backoff） | bpc（單一4-gram） |
|---|---|---|---|---|
| 193 MiB | 2^16 | 4 | **2.585** | 3.868 |
| 515 MiB | 2^18 | 4 | **2.585** | 3.868 |
| **2,057 MiB（2GB）** | **2^20** | 4 | **2.585** | 3.868 |

**結論：模型放大 16 倍（193→2057 MiB），bpc 完全相同。**
放大模型到 2GB 對預測品質**無效**——無論單一 gram 或 backoff 都是平線。
但 backoff 把基線從 3.87 拉低到 2.59（33% 改善）；而**加高階數**（backoff
1-4→1-5）再降到 2.33——**提升來自更好的底層原理（多階利用），不是更大
的模型**。

**根因**：槽位一旦飽和（256MiB 語料已灌滿 2^16 槽），再多槽位只是
複製相同統計，bpc 由「語料統計密度」與「底層原理」決定，不由槽位數。

> ⚠️ **「模型迭代至 2GB」仍是錯誤方向**——純放大槽位是浪費記憶體。
> 正確的縮放方向是**語料量**（bpc 隨語料改善）+ **更好的底層原理**
> （backoff 已證實：相同語料，bpc 3.87→2.46；加高階數再降到 2.33）。

---

## 4. 是否在正確路線？

**是——backoff 證明底層原理決定成效，與步數/硬體無關。**

✅ 正確的部分：
- **多階 backoff（5→4→3→2→1）是最優底層原理**：零梯度步、純統計，
  bpc 3.73→2.33，**超過 gzip 與 bz2**。這證實用戶的判斷：學習原理優劣
  決定所需步數，不是硬體決定。Kneser-Ney 實測無優勢（hash 碰撞破壞
  續接計數）。
- **固定大小 + 真實記憶體**：誠實，壓縮比 3.71x 超過 gzip
- **單一核心多模態**（文字/影像/音訊同 byte 流）是對的架構
- **留出集評估**是對的方法論
- **確定性層標註「非 AI」**是誠實的

❌ 錯誤的部分（已被實驗修正）：
- **「需要更大神經層才能打破 bpc」是錯的**：問題在單一 4-gram 浪費了
  低階資訊，不是模型太小。backoff 用 0 步梯度就解決了。
- **「模型迭代至 2GB」無效**：193→2057MiB bpc 平線（2.585）
- **統計層邏輯（0.626）**證明 byte 統計學不到推理

---

## 5. 可改善點（按優先序）

1. **已試過並收斂**：Kneser-Ney 平滑在 hash 表架構下實測無優勢（2.592 vs
   backoff 2.381）——hash 碰撞破壞精確續接計數，PPMd 的 1.48 需要精確樹。
   已試過並收斂：**6-gram backoff 變差**（2.724，碰撞+稀疏）——5 階是甜蜜點。
2. **語料放大**（最高優先）：bpc 隨語料量改善（90MB→2.33，900MB→2.38 留出集）。
   更大的標準語料（如 wiki 全量）可逼近 lzma 2.178。
3. **疊真神經層**：在 backoff 統計之上疊固定維度可學權重（SharedLatentSpace
   梯度技術），做長上下文 + 向量泛化——但現在 bpc 已超過 gzip 與 bz2，此步
   是衝擊 PPMd/LSTM 的 1.3-1.5 bpc，非必要急迫。
4. **BPE 詞彙化**：byte→token 降序列長，可提升語義捕捉（增模型大小，
   需權衡）。
5. **統計層邏輯**：改用真分類器（如邏輯回歸 over 特徵）取代純統計，
   可能從 0.626 提升到 ~0.7+。
6. **推理向量化**：逐 byte 推理 4,400 bytes/s 太慢；批量處理可提升
   10–100x。

---

## 6. 誠實結論（2026-08-22 污染修正後）

- **統一引擎仍是「誠實且有用」的模型**：固定 **259MiB**、可驗證、CPU 3.3 MiB/s、
  **乾淨 bpc 2.403 超過 gzip(2.951)**、多模態統一、**對話算術/邏輯 1.0** 確定性、
  生成非過擬合（10-13 byte 最長匹配）。
- **⚠️ 留出集污染已修正**：舊 2.331/2.338 是訓練循環溢位（4MB chunk 讀到
  96.4MB，90MB 處「留出集」在訓練區內）的污染值。乾淨基準：90MB→**2.403**，
  full_train 真留出集→2.711。「超過 bz2」宣稱撤回（差距 3%）。
- **底層原理比硬體重要**：backoff 5→1（0 梯度）的相對改善仍然成立。
- **數據集位置已配置化**：`UNIFIED_DATA_ROOT=/media/covo/ZX`。
- 仍輸 bz2/lzma/PPMd/LLM；下一步：語料放大（wiki 全量乾淨抽取）+ W 分離。