# SUPERSEDED — Archived 2026-08-13

This folder (`docs/03-technical-architecture/memory-systems/`) was archived because its
contents describe the **`src/core_ai/` module layout, which no longer exists** (the code
was restructured into `apps/backend/src/ai/` and `apps/backend/src/core/`).

The HAM memory system now lives under `apps/backend/src/ai/memory/ham_memory/`.
Content is preserved here for historical reference only. Do not link to these files
from current documentation.
