{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HSPBeliefPayload_v0.1",
  "description": "Schema for HSP Belief Payload version 0.1. Similar to Fact, but for statements with higher subjectivity or uncertainty.",
  "type": "object",
  "allOf": [
    { "$ref": "Fact_v0.1.json" }
  ],
  "properties": {
    "belief_holder_ai_id": {"type": "string", "description": "The AI holding this belief (defaults to source_ai_id if not explicitly provided by sender)."},
    "justification_type": {"type": "string", "enum": ["text", "inference_chain_id", "evidence_ids_list"], "description": "Type of justification provided."},
    "justification": {"type": ["string", "array"], "items": {"type": "string"}, "description": "Reasoning, link to an inference chain, or list of supporting fact/belief IDs."}
  },
  "required": ["belief_holder_ai_id"]
}
