{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "HSPAcknowledgementPayload_v0.1",
  "description": "Schema for HSP Acknowledgement Payload version 0.1. Used to acknowledge receipt or processing of a message.",
  "type": "object",
  "properties": {
    "status": {"type": "string", "enum": ["received", "processed"], "description": "Status of the acknowledgement."},
    "ack_timestamp": {"type": "string", "format": "date-time", "description": "ISO 8601 UTC timestamp of when the acknowledgement was generated."},
    "target_message_id": {"type": "string", "format": "uuid", "description": "The message_id of the HSP message being acknowledged."}
  },
  "required": ["status", "ack_timestamp", "target_message_id"]
}
