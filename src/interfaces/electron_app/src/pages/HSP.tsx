import React from 'react';

const HSP = () => {
  return (
    <div>
      <h1>HSP Network</h1>
      <p>This is the HSP network interface.</p>
      {/* The original HSP interface from renderer.js and index.html would be implemented here as a React component. */}
    </div>
  );
};

export default HSP;
