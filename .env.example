# =============================================================================
# Angela AI - Environment Configuration
# =============================================================================
# 将此文件复制为 .env 并更新为您的设置
# 运行前请先运行: python -m src.core.config_validator 验证配置
#
# 安装步骤:
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，更新必需的配置
# 3. 运行验证: python -m src.core.config_validator
# =============================================================================

# -----------------------------------------------------------------------------
# 应用设置
# -----------------------------------------------------------------------------
ANGELA_ENV=development          # 运行环境: development, production, testing
NODE_ENV=development           # Node.js 运行环境
ANGELA_TESTING=true            # 启用测试模式

# -----------------------------------------------------------------------------
# 后端配置
# -----------------------------------------------------------------------------
BACKEND_HOST=127.0.0.1         # 后端服务器地址
BACKEND_PORT=8000              # 后端服务器端口 (1-65535)
BACKEND_URL=http://127.0.0.1:8000  # 后端服务器完整 URL

# -----------------------------------------------------------------------------
# 安全密钥 (生产环境必须生成真实的密钥)
# -----------------------------------------------------------------------------
# 使用以下命令生成密钥: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ANGELA_KEY_A=your_key_a_minimum_32_chars    # 后端控制密钥 (最少32字符)
ANGELA_KEY_B=your_key_b_minimum_32_chars    # 移动通信密钥 (最少32字符)
ANGELA_KEY_C=your_key_c_minimum_32_chars    # 桌面同步密钥 (最少32字符)

# -----------------------------------------------------------------------------
# 数据库配置
# -----------------------------------------------------------------------------
DATABASE_URL=sqlite:///./angela.db          # 数据库连接 URL
DATABASE_POOL_SIZE=10                       # 连接池大小
DATABASE_MAX_OVERFLOW=20                    # 最大溢出连接数

# PostgreSQL 示例:
# DATABASE_URL=postgresql://user:password@localhost:5432/angela

# -----------------------------------------------------------------------------
# Live2D 配置
# -----------------------------------------------------------------------------
LIVE2D_MODEL_PATH=./resources/models/miara_pro_t03.model3.json  # Live2D 模型路径
LIVE2D_SDK_CDN=https://cubism.live2d.com/sdk-web/cubismcore/live2dcubismcore.min.js  # Live2D SDK CDN

# -----------------------------------------------------------------------------
# 性能设置
# -----------------------------------------------------------------------------
PERFORMANCE_MODE=auto          # 性能模式: auto, low, medium, high, ultra
TARGET_FPS=60                  # 目标帧率 (30-144)
ENABLE_HARDWARE_ACCELERATION=true  # 启用硬件加速

# 性能模式说明:
# - low: 30 FPS, 基础效果, 适合低配设备
# - medium: 45 FPS, 标准效果
# - high: 60 FPS, 增强效果
# - ultra: 120+ FPS, 所有效果, 适合高配设备

# -----------------------------------------------------------------------------
# 日志配置
# -----------------------------------------------------------------------------
LOG_LEVEL=info                 # 日志级别: debug, info, warning, error, critical
LOG_FILE=./logs/angela.log     # 日志文件路径
LOG_MAX_SIZE=10MB              # 日志文件最大大小
LOG_BACKUP_COUNT=5             # 保留的日志文件数量

# -----------------------------------------------------------------------------
# 功能开关
# -----------------------------------------------------------------------------
ENABLE_VOICE_RECOGNITION=true  # 启用语音识别
ENABLE_TTS=true                # 启用语音合成
ENABLE_WEBSOCKET=true          # 启用 WebSocket
ENABLE_MOBILE_BRIDGE=true      # 启用移动端桥接

# -----------------------------------------------------------------------------
# 外部服务
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your_openai_api_key_here      # OpenAI API 密钥
GOOGLE_API_KEY=your_google_api_key_here      # Google API 密钥

# 获取 API 密钥:
# - OpenAI: https://platform.openai.com/api-keys
# - Google: https://console.cloud.google.com/apis/credentials

# -----------------------------------------------------------------------------
# 开发设置
# -----------------------------------------------------------------------------
DEBUG_MODE=true                # 调试模式 (生产环境应为 false)
HOT_RELOAD=true                # 热重载
ENABLE_CORS=true               # 启用 CORS

# -----------------------------------------------------------------------------
# 测试配置
# -----------------------------------------------------------------------------
TEST_MODE=true                 # 测试模式
MOCK_EXTERNAL_APIS=true        # 模拟外部 API
TEST_DATA_PATH=./test_data     # 测试数据路径

# -----------------------------------------------------------------------------
# 高级配置 (可选)
# -----------------------------------------------------------------------------
# WebSocket 配置
WS_MAX_CONNECTIONS=100         # 最大 WebSocket 连接数
WS_HEARTBEAT_INTERVAL=30       # 心跳间隔 (秒)

# 缓存配置
CACHE_ENABLED=true             # 启用缓存
CACHE_TTL=3600                 # 缓存过期时间 (秒)

# 矢量数据库配置
VECTOR_DB_TYPE=chromadb        # 矢量数据库类型: chromadb, faiss
VECTOR_DB_PATH=./data/vector_db  # 矢量数据库路径