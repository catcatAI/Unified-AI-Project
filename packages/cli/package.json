{
  "name": "cli",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cross-env PYTHONPATH=../backend/src python -m cli.main",
    "test": "echo 'CLI tests skipped - requires backend dependencies'",
    "test:coverage": "echo 'CLI coverage skipped - requires backend dependencies'"
  }
}
