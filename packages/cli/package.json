{
  "name": "cli",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python main.py",
    "test": "PYTHONPATH=$(pwd)/../backend pytest tests",
    "test:coverage": "pytest --cov=cli --cov-report=html --cov-report=term-missing tests"
  }
}
