from unittest.mock import MagicMock, patch

import numpy as np
import pytest

# This file is intentionally left blank to avoid import errors
# when running tests without all optional dependencies installed.
